import { Text } from "@mantine/core";

export default function ResistorCalculator() {
  return (
    <Text>
      Welcome to the calculator tools! Select a tool from the tabs above to get
      started.
    </Text>
  );
}
